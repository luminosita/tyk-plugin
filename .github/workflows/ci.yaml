name: Tyk Grpc Plugin
on:
  push:
    branches: [main]
permissions:
  contents: read 
jobs:
  test:
    strategy:
      matrix:
        os: [luminosita-ubuntu]
        go: ["1.23"]  
    name: ${{ matrix.os }} @ Go ${{ matrix.go }}
    runs-on: luminosita-ubuntu
    steps:
      - run: echo " The na me of your branch is ${{ GitHub.ref }} and your repository is ${{ GitHub.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo " The ${{ GitHub.repository }} repository has been cloned to the runner." 
      # - uses: addnab/docker-run-action@v3
      #   with:
      #     image: tykio/tyk-plugin-compiler:v5.2.1
      #     options: -v ${{ github.workspace }}:/plugin-source --platform=linux/amd64
      #     run: |
      #             echo "Hello World"
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: false
          tags: luminosita/tyk-plugin:latest
      - name: List files in the working directory
        run: |
          ls
      - run: echo " This job's status is ${{ job.status }}."